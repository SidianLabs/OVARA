# Prompt Injection

Prompt injection is dangerous because it can redirect legitimate authority into
unauthorized actions. Ovara mitigates this by validating actions at runtime
instead of trusting model intent.

