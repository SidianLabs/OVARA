# Policy API

## `POST /v1/policies/evaluate`

Evaluate an action against a specific policy set.

## `POST /v1/policies/simulate`

Dry-run a proposed policy change against sample actions.

## `GET /v1/policies/:id`

Fetch policy document and version metadata.

