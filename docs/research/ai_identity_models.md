# AI Identity Models

Ovara should evaluate identity models that separate stable identity roots from
ephemeral runtime instances, allowing attribution without over-granting
authority to every execution context.

