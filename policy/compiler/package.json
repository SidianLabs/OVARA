{
  "name": "@ovara/policy-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ovara policy compiler — validates and compiles policy JSON into evaluator format",
  "main": "dist/index.js",
  "scripts": { "build": "tsc", "test": "vitest run" },
  "dependencies": { "zod": "^3.23.0" },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.0.0", "@types/node": "^22.0.0" }
}
